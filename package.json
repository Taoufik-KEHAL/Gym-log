{
  "name": "gym-log",
  "version": "1.0.0",
  "private": true,
  "description": "Local-first gym log: weight, workouts, calories, protein.",
  "scripts": {
    "sync": "cap sync android",
    "open:android": "cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^8.4.2",
    "@capacitor/core": "^8.4.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.4.2"
  }
}
